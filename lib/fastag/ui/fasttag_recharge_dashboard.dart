
import 'package:flutter/material.dart';

class FastTagRechargeDashboard extends StatelessWidget {
  const FastTagRechargeDashboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
